# 问题三 v2.2 服务器执行与审核说明

## 摘要

本次任务完成了问题三可解释性方案 v2.2 的服务器端独立重跑、参数冻结、
附件4推理、预测器不变性检查、独立验收和审计归档。v2.2 的核心修正是
将局部 top-support 比较改为**对称的独立 top-vs-top 选择**：连续窗口和
控制模式分别在自己的候选集合内按实际 masked-position budget 归一化后
取最大值，再配对比较，消除了 v2 中单边择优带来的偏差。

最终结果：

- 三尺度验证全部完成，退出码均为 0；
- `W=3` 是唯一同时通过 continuous-vs-random 与
  continuous-vs-point-scatter 两组视频级聚类 95% CI 门槛的尺度；
- 最终冻结窗口为 `W=3`；
- 附件4 20 个样本全部完成推理，MFA 对齐 20/20，比例回退 0；
- 附件4 极性和强度相对 v2 逐值不变，最大差异为 `0.0`；
- `verification.json` 状态为 `PASS`，受保护基线差异为 `clean`；
- 审计 ZIP 为 3,996,851 字节，SHA-256 为
  `35e93f87d3c27aa18991f59ae085d77199d705da5ff7232befbd621ee6ff33c4`。

## 1. 版本与基线

- Git 提交：`808b602ac054540947772fac7f9906f35e0b5f13`
- 扩展：`问题三/q3_explainability_v2_2_20260926`
- 协议：`configs/protocol.json`
- 协议 SHA-256：
  `831d0079b543b7280ea51a3770de11d9a96920ab096aca498e1c264b97769637`
- 预测器：`M3-ensemble9 + C2`，9 个冻结 checkpoint，未训练、未替换；
- 服务器：`myserver`，设备 `cuda:2`；
- 共享环境未安装或升级依赖；仅通过
  `PYTHONPATH=/data2/hy/q3_runs/q3_explainability_v2_20260925/python_deps`
  使用既有隔离依赖。

## 2. 三尺度验证与冻结

三份验证报告的 schema 均为
`q3v2.2-symmetric-top-support-v1`，且
`control_selection_symmetric=true`。

| W | continuous | random-contiguous | point-scatter | continuous-random 95% CI | continuous-scatter 95% CI | 合格 |
|---:|---:|---:|---:|---|---|---|
| 3 | 0.053386397863017875 | 0.04202510709329737 | 0.04423371006939197 | [0.008098506908349401, 0.014758199750378854] | [0.004684681667907746, 0.014318634709801025] | 是 |
| 5 | 0.036516363897233624 | 0.030037903027601547 | 0.033294696823331846 | [0.004103997697817101, 0.008764402492685548] | [-0.0006959137712652495, 0.006301216831072192] | 否 |
| 10 | 0.020923288929501127 | 0.01912397106061157 | 0.019480478667992084 | [0.00019689033650811645, 0.0033859033855972095] | [-5.1999747196204575e-05, 0.0029712869349022312] | 否 |

`W=3` 是唯一合格尺度，因此选择器选择 `W=3`；该决定在附件4读取前
完成并写入 `server_outputs/freeze_parameters.json`。`W=5` 和 `W=10`
的 continuous-scatter CI 下界均小于 0，故不能以“分数较高”为由入选。

验证集预测指标（三个尺度一致）：

| 指标 | 数值 |
|---|---:|
| Accuracy | 0.6208791208791209 |
| Macro-F1 | 0.5997068114591327 |
| MAE | 0.5799670219421387 |
| Pearson | 0.6635369958302701 |
| n | 728 |

## 3. 附件4结果与不变性

附件4推理严格发生在冻结之后，报告明确记录
`frozen_explanation_parameters=true` 和
`attachment4_used_for_tuning=false`。

- 样本数：20；
- 有效 sample-modality 统计：59；
- 连续窗口扰动数：566；
- 推理耗时：22.950748920440674 秒；
- MFA 对齐：20/20；
- 比例回退：0；
- 极性变化：0；
- 最大强度差：0.0；
- 最大 log-prob 差：0.0；
- 主要模态变化：0；
- 证据变化：0。

主要模态分布为 text 16、audio 1、vision 3；关键证据数量为 text 40、
audio 31、vision 33。说明本次修正只改变验证阶段的比较统计与冻结依据，
没有改变附件4的冻结预测或已选证据。

## 4. 验收结论

`server_outputs/verification.json`：

- `status=PASS`
- `source_manifest.files=26`
- `source_manifest.manifest_sha256=400028a469ac40255250428f151921eb910c512e211840b124ab1b53f307224b`
- `submission_bytes=321284`
- `attachment4_samples=20`
- `mfa_aligned_samples=20`
- `fallback_samples=0`
- `selected_window_size=3`
- `protected_baseline_diff=clean`

审计包 `audit_export/q3_explainability_v2_2_audit.zip` 已通过 ZIP CRC 和
成员哈希复核；`audit_export/SHA256SUMS.txt` 在本地再次校验通过。

## 5. 目录说明

- `server_outputs/`：服务器完整运行目录的本地镜像，包括验证报告、冻结
  参数、附件4 raw/打包结果、日志和独立验收 JSON；
- `server_outputs/submission/`：可直接提交的四个文件：
  `q3_predictions_explanations.csv`、`q3_evidence.jsonl`、
  `q3_validation_report.json`、`q3_attachment4_report.json`；
- `audit_export/`：独立审计 ZIP、索引和 SHA-256 校验文件；
- `records/EXECUTION_RECORD.md`：执行时间线、关键数值和复现入口；
- `scripts/`：本次服务器运行使用的验证与后处理脚本副本；
- `PUSH_MANIFEST_SHA256.txt`：本目录所有回传文件的 SHA-256 清单。

## 6. 审核建议

1. 先检查 `server_outputs/freeze_parameters.json` 中的
   `selected_window_size` 和 `selection_mode`；
2. 再检查 `server_outputs/validation/w3|w5|w10/` 三份报告中的
   `comparison_schema`、`control_selection_symmetric` 和两组 CI；
3. 检查 `server_outputs/result_comparison.json` 的预测器不变性；
4. 检查 `server_outputs/verification.json` 是否为 PASS；
5. 最后检查 `audit_export/SHA256SUMS.txt` 和审计 ZIP。