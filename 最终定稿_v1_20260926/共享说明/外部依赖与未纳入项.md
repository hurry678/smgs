# 外部依赖与未纳入项

## 未重复纳入

1. 题给附件1至附件4及原始视频：属于竞赛原始输入，体积较大且可能受分发
   约束。本目录保存其派生特征、预测、解释、摘要和输入 SHA-256。
2. Python/Conda 虚拟环境、CUDA、MFA、FFmpeg 和系统库：属于运行环境，
   版本与调用方式已记录在各问 README、日志和审计文件中。
3. Q1 的 Whisper、MediaPipe 与 MFA 资源：正式交付包保留来源和锁定摘要，
   未将可重新下载的完整模型与环境重复打包。
4. 可重建中间数据，例如服务器上的 `q2_data.npz`：冻结清单保留路径及
   SHA-256，正式结果与审计证据已纳入。

## 已纳入但保持压缩

- Q1 完整定稿 ZIP，并另提供可直接浏览的解压内容。
- Q2 九个冻结 checkpoint：审计 `part01`。
- Q2 共享 MiniLM 编码器：审计 `part03`。
- Q2 全量搜索、确认与发布证据：三卷审计 ZIP。
- Q3 最终独立审计 ZIP，以及未压缩的服务器提交与验证结果。

## 复现边界

本目录支持检查正式代码、结果、选择依据、哈希和已归档权重。若从原始视频
重新执行 Q1，或从题给特征重新构建 Q2/Q3 输入，仍需提供对应竞赛附件。
附件3和附件4无标签，不得据此报告 Accuracy、Macro-F1、MAE 或 Pearson。
