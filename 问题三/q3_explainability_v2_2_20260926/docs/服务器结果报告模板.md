# 问题三 v2.2 服务器结果报告

> 状态：待服务器执行，不得预填实验数值。

## 1. 环境与完整性

| 项目 | 结果 |
|---|---|
| Git commit | `<commit>` |
| GPU / CUDA / PyTorch | `<value>` |
| 验证预检 | `<PASS/FAIL>` |
| 附件4预检 | `<PASS/FAIL>` |
| Q2 冻结清单 SHA | `<sha256>` |
| Q2 版本清单 SHA | `<sha256>` |
| 9 个 checkpoint | `<9/9 PASS>` |
| MiniLM 权重 | `<PASS/FAIL>` |
| 复用附件4 NPZ / offsets / MFA audit SHA | `<PASS/FAIL>` |

## 2. 验证集尺度比较

| 窗口 | 连续 top | 随机连续 top | 点散 top | 两组对称 95% CI | 双门槛 |
|---:|---:|---:|---:|---|---|
| 3 | `<value>` | `<value>` | `<value>` | `<CI-random>; <CI-scatter>` | `<是/否>` |
| 5 | `<value>` | `<value>` | `<value>` | `<CI-random>; <CI-scatter>` | `<是/否>` |
| 10 | `<value>` | `<value>` | `<value>` | `<CI-random>; <CI-scatter>` | `<是/否>` |

- 比较 schema：`q3v2.2-symmetric-top-support-v1`
- 冻结窗口：`<value>`
- 选择模式：`<clustered_ci_eligible_best_score / preregistered_middle_scale_fallback>`
- 完整验证集 Accuracy / Macro-F1 / MAE / Pearson：`<values>`
- 三个报告是否覆盖完全相同的 728 条：`<是/否>`
- 相对 v2：`<窗口是否改变；门槛结论是否改变>`

## 3. 模态归因

| 指标 | text | audio | vision |
|---|---:|---:|---:|
| 分类平均绝对份额 | `<value>` | `<value>` | `<value>` |
| 强度平均绝对份额 | `<value>` | `<value>` | `<value>` |
| 附件4主要参考模态计数 | `<value>` | `<value>` | `<value>` |

| 两两交互 | 分类平均有符号值 | 分类平均绝对值 | 强度平均有符号值 | 强度平均绝对值 |
|---|---:|---:|---:|---:|
| text-audio | `<value>` | `<value>` | `<value>` | `<value>` |
| text-vision | `<value>` | `<value>` | `<value>` | `<value>` |
| audio-vision | `<value>` | `<value>` | `<value>` | `<value>` |

- 分类 Shapley 最大加和误差：`<value>`
- 强度 Shapley 最大加和误差：`<value>`
- Shapley 与 LOO 最大影响模态一致率：`<value>`

## 4. 附件4冻结推理

- 样本覆盖：`<20/20>`
- 极性计数：Negative `<n>`，Neutral `<n>`，Positive `<n>`
- 强度范围：`<min, max>`
- MFA 成功：`<n>/20`
- 比例回退样本：`<IDs 或 无>`
- text/audio/vision 映射状态计数：`<values>`
- 附件4是否用于调参：`否`
- 相对 v2 的极性变化数 / 最大强度差：`<必须为 0 / 必须为 0>`
- 相对 v2 的局部证据变化数：`<value>`

## 5. 运行资源

| 阶段 | 耗时 | 峰值内存 | 峰值显存 | 退出码 |
|---|---:|---:|---:|---:|
| validation w3 | `<value>` | `<value>` | `<value>` | `<value>` |
| validation w5 | `<value>` | `<value>` | `<value>` | `<value>` |
| validation w10 | `<value>` | `<value>` | `<value>` | `<value>` |
| attachment4 | `<value>` | `<value>` | `<value>` | `<value>` |

## 6. 最终验收

- `verification.json.status`：`<PASS/FAIL>`
- 提交目录字节数：`<value>`
- 审计 ZIP SHA-256：`<sha256>`
- 审计 ZIP CRC：`<PASS/FAIL>`
- `ds/` 与 `优化/q2yhv2_1/`：`<未修改/异常>`
- 未读取 official test/附件3做选择：`<确认>`
- 附件4产物在 v2.2 参数冻结后才复用：`<确认>`

## 7. 局限性

保留以下边界：Shapley 与遮挡是模型行为归因，不是现实因果；MFA 是自动
强制对齐，不是人工时间真值；比例回退只能作为近似定位；附件4无标签，不能
报告 Accuracy、F1、MAE 或 Pearson。
